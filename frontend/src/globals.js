const GlobalVariables = {
    apiURL: 'http://localhost:8000/api/',
    imgURL: 'http://localhost:8000',    // the image URLs start with a slash, so we leave it off here
};
export default GlobalVariables;